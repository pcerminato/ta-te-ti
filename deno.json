{
  "tasks": {
    "dev": "deno run -A npm:vite",
    "build": "deno run -A npm:tsc && vite build",
    "preview": "deno run -A npm:vite preview"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  },
  "compilerOptions": {
    "lib": [
      "deno.window",
      "dom"
    ]
  }
}
